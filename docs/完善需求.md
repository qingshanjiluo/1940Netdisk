# 完善需求清单

> ✅ = 已完成 &nbsp;|&nbsp; 🔄 = 进行中 &nbsp;|&nbsp; ⏳ = 待开始

---

✅ **1. 首页左上角明暗主题转换按钮删除**
   - 修改文件: [`index.html`](../index.html:517)
   - 摘要: 移除了导航栏中的 `<button data-theme-toggle>` 元素

✅ **2. WebDAV面板UI进行优化，修改左上角ico和文字显示优化**
   - 修改文件: [`webdav.html`](../webdav.html:553)
   - 摘要: 将 `<img>` logo 替换为 Font Awesome 图标 `<i class="fas fa-hard-drive">`，使用金色 `var(--gold)` 主题色

✅ **3. 统一顶端栏在多个页面**
   - 修改文件:
     - [`index.html`](../index.html:522) — 导航栏改为 上传、图库、管理、WebDAV
     - [`gallery.html`](../gallery.html:819) — 统一导航链接格式
     - [`admin-waterfall.html`](../admin-waterfall.html:337) — 增加完整导航链接
     - [`webdav.html`](../webdav.html:558) — 统一导航链接格式
   - 摘要: 4个页面的导航栏统一为 `<i class="fas fa-xxx"></i><span>标签</span>` 格式

✅ **4. 身份组管理删除面板设置功能，改为身份组内置权限**
   - 修改文件:
     - [`admin.html`](../admin.html:907) — 表格列头、权限显示、模态框
     - [`functions/api/admin/groups.js`](../functions/api/admin/groups.js:39) — POST 接收 `builtinPerms`
     - [`functions/api/admin/groups/[id].js`](../functions/api/admin/groups/[id].js:33) — PATCH 更新 `builtinPerms`
   - 内置权限: 上传、读取文件、下载文件、管理文件、删除文件、新建一般用户、新建管理员、新建身份组、修改他人身份组

✅ **5. 完善个人主页功能，可以修改个人资料、密码、头像**
   - 修改文件:
     - [`functions/api/profile.js`](../functions/api/profile.js) — 新增个人资料 API（GET 获取、PATCH 更新）
     - [`functions/api/auth/check.js`](../functions/api/auth/check.js:49) — 返回信息增加 `id`、`avatar` 字段
     - [`index.html`](../index.html:2113) — `saveProfile` 从 localStorage 改为 PATCH `/api/profile`
     - [`index.html`](../index.html:1087) — 个人资料模态框（头像 URL、昵称、密码修改）
   - 后端: GET `/api/profile` 返回用户信息；PATCH `/api/profile` 支持修改 `nickname`、`avatar`、`password`
   - 密码修改需要验证 `currentPassword`，经过 SHA-256 哈希后与服务器比对

✅ **6. 修复完善管理页面删除文件夹、文件功能，目前点击删除无反应**
   - 修改文件:
     - [`gallery.html`](../gallery.html:1307) — `deleteImage` 方法 `method: 'GET'` → `method: 'POST'`
     - [`gallery.html`](../gallery.html:1227) — `batchDelete` 方法 `method: 'GET'` → `method: 'POST'`
     - [`admin.html`](../admin.html:2744) — `handleDelete` 已为 `'POST'`，无需修改
     - [`admin-imgtc.html`](../admin-imgtc.html:764) — `handleDelete` 方法 `method:'GET'` → `method:'POST'`
     - [`admin-imgtc.html`](../admin-imgtc.html:813) — 批量删除 `method:'GET'` → `method:'POST'`
   - 根因: Cloudflare Functions `onRequest(context)` 虽然可以接受任何 HTTP 方法，但前端使用 `method:'GET'` 不符合 REST 语义且可能在某些代理/CDN 层被缓存或拦截

✅ **7. 修复报错 flyfish-file-viewer CDN 404**
   - 文件: [`preview.html`](../preview.html:16)
   - 修复: 将 `https://cdn.jsdelivr.net/npm/flyfish-file-viewer@latest/dist/viewer.umd.min.js` 替换为 `https://cdn.jsdelivr.net/npm/flyfish-file-viewer@1.3.0/dist/viewer.umd.min.js`
   - 根因: `@latest` 指向的版本不存在或被移除，锁定到 `1.3.0` 版本

✅ **8. 完善文件夹上传子文件夹路径问题**
   - 修改文件:
     - [`index.html`](../index.html:590) — `handleFolderSelect` 方法
   - 修复: 完善 `webkitRelativePath` 解析逻辑，提取文件相对于所选文件夹的路径作为 `subfolder`
   - 上传时将 `subfolder` 作为 `folder` 参数传给 API，确保子文件夹结构在服务器端正确保留

✅ **9. 部分按钮UI为web原生，进行修复完善**
   - 修改文件:
     - [`index.html`](../index.html:304) — 新增以下 CSS 覆盖:
       ```css
       select.input-glass,
       .modal-form select { appearance:auto; -webkit-appearance:auto; ... }
       .modal-form input[type="checkbox"],
       .checkbox-wrap input[type="checkbox"] { accent-color:var(--olive); ... }
       input[type="file"] { color:var(--text-primary); font-size:.82em; }
       ```
   - 影响: `<select>` 下拉箭头恢复系统原生样式但仍保持玻璃态背景；checkbox 使用主题色 `--olive`；file input 文字颜色与主题协调
   - **待办**: [`gallery.html`](../gallery.html) 和 [`admin-imgtc.html`](../admin-imgtc.html) 同样存在原生 select/checkbox，尚未添加相同 CSS

✅ **10. 修复首页右上角个人信息下拉栏闪现消失问题**
   - 修改文件:
     - [`index.html`](../index.html:547) — 用户下拉菜单已修复（上一轮）
     - [`gallery.html`](../gallery.html:829) — 用户下拉 `@blur`+`setTimeout(200)` → `@mouseenter`/`@mouseleave`+`@click.stop`
     - [`admin.html`](../admin.html:629) — 用户下拉 `@blur`+`setTimeout(200)` → `@mouseenter`/`@mouseleave`+`@click.stop`
     - 移除方法: `handleUserDdBlur` (admin.html 和 gallery.html)
   - 根因: `@blur` 在菜单内的可点击元素获得焦点时也会触发，导致下拉先关闭。`setTimeout(200)` 延迟关闭造成闪现
   - 方案: 改用 `@mouseenter` 展开 / `@mouseleave` 收起 + 菜单内容 `@click.stop` 防止冒泡

---

## 🔄 额外发现并修复的语义问题

✅ **所有写操作的 HTTP 方法规范化（GET → POST）**
   - 以下操作虽然后端 `onRequest(context)` 接受任意方法，但前端错误使用了 `method:'GET'`，现已全部改为 `method:'POST'`：
   - [`admin.html`](../admin.html:2719) — `toggleLike`: `GET` → `POST`
   - [`admin.html`](../admin.html:2733) — `editName`: `GET` → `POST`
   - [`admin.html`](../admin.html:2837) — 批量 block/white: `GET` → `POST`
   - [`admin-imgtc.html`](../admin-imgtc.html:750) — `toggleLike`: `GET` → `POST`
   - [`admin-imgtc.html`](../admin-imgtc.html:849) — 批量 block/white: `GET` → `POST`
   - [`admin-imgtc.html`](../admin-imgtc.html:946) — `editName`: `GET` → `POST`
   - [`gallery.html`](../gallery.html:1307) — `deleteImage`: `GET` → `POST`
   - [`gallery.html`](../gallery.html:1227) — `batchDelete`: `GET` → `POST`

✅ **认证响应扩展**
   - [`functions/api/auth/check.js`](../functions/api/auth/check.js:49) — `userInfo` 增加 `id` 和 `avatar` 字段
   - 前端 `checkAuth` 方法同步存储新字段

---

## 📋 建议进一步优化的项目

⏳ **11. gallery.html 和 admin-imgtc.html 原生控件样式统一**
   - 问题: [`index.html`](../index.html:304) 已添加 `<select>`、`<input type="checkbox">`、`<input type="file">` 的玻璃态样式覆盖，但 [`gallery.html`](../gallery.html) 和 [`admin-imgtc.html`](../admin-imgtc.html) 尚未添加
   - 建议: 将 CSS 复制到对应的 `<style>` 块中

⏳ **12. 图片懒加载（loading="lazy"）**
   - 问题: [`gallery.html`](../gallery.html:885) 的 `<img>` 标签没有 `loading="lazy"` 属性
   - 涉及文件: `gallery.html`、`admin.html`（网格视图）、`admin-imgtc.html`
   - 建议: 在所有 `<img>` 标签上添加 `loading="lazy"`，减少初始加载时的带宽消耗

⏳ **13. API 调用封装统一错误处理**
   - 问题: 当前所有页面使用裸 `fetch()` 调用，每个 API 调用的 `.catch()` 处理方式不一致（有的显示 toast、有的静默失败、有的 `console.error`）
   - 涉及文件: `index.html`、`gallery.html`、`admin.html`、`admin-imgtc.html`
   - 建议: 封装统一的 `apiRequest(url, options)` 函数，内置:
     - 超时控制（`AbortController`）
     - 401 自动跳转登录
     - 统一 toast 错误提示
     - JSON 响应自动解析

⏳ **14. 前端响应式图片 & 缩略图**
   - 问题: `gallery.html` 中直接加载原图 `/file/{name}`，大图片加载慢
   - 建议: 后端增加缩略图生成端点（如 `/file/{name}?thumb=1`），前端先加载缩略图，点击后再加载原图

⏳ **15. admin-waterfall.html 和 block-img.html 代码审查**
   - 问题: 本轮优化未覆盖 [`admin-waterfall.html`](../admin-waterfall.html) 和 [`block-img.html`](../block-img.html)
   - 这两个页面可能存在相同的 `method:'GET'` 写操作、下拉闪现等问题
   - 建议: 逐一审查并修复

⏳ **16. 上传队列持久化到 localStorage**
   - 问题: 当前上传队列 `uploadingFiles` 是内存状态，页面刷新后丢失
   - 建议: 使用 `localStorage` 持久化上传队列状态（文件列表、进度），刷新后恢复

⏳ **17. 分片上传进度合并计算**
   - 问题: 分片上传的进度计算未正确聚合各分片进度
   - 涉及: [`functions/api/chunked-upload/`](../functions/api/chunked-upload/)
   - 建议: 前端计算整体进度时遍历所有已上传分片的 `uploadedChunks` 与总 `chunkCount`

⏳ **18. 前端构建/Bundling 可行性**
   - 问题: 当前所有页面是单 HTML 文件 + CDN 引入 Vue 2 的方式，没有构建步骤
   - 建议: 考虑引入 Vite 或 esbuild 进行打包，实现:
     - Vue SFC 组件化开发
     - CSS/JS 压缩和 Tree Shaking
     - 环境变量注入
     - 按需加载（Code Splitting）

⏳ **19. 安全性增强 — 前端敏感信息过滤**
   - 问题: 前端在某些错误响应中可能直接显示服务器返回的原始错误信息，可能泄露内部路径或 KV key
   - 建议: 后端统一错误响应格式，生产环境不返回内部细节；前端对显示的错误信息做 sanitize

⏳ **20. 统一 Loading / Skeleton 组件**
   - 问题: 目前各个页面加载数据时的 loading 状态不统一（有的用旋转图标、有的用文字提示、有的无指示）
   - 建议: 统一为 Skeleton 骨架屏 + 玻璃态加载指示器，提升用户体验

⏳ **21. 删除版块管理（sections）面板**
   - 涉及文件: [`admin.html`](../admin.html:967) — 版块管理面板（sections tab）
   - 后端: [`functions/api/admin/sections.js`](../functions/api/admin/sections.js)、[`functions/api/admin/sections/[id].js`](../functions/api/admin/sections/[id].js)
   - 原因: 版块管理功能目前未在项目核心流程中使用，后台管理界面中的 "版块管理" Tab 和相关代码造成了不必要的界面复杂度
   - 建议:
     1. 移除 [`admin.html`](../admin.html:659) 中的版块 nav tab
     2. 移除 [`admin.html`](../admin.html:967-997) sections 面板的 HTML 模板
     3. 移除 admin CRUD 模态框中的 section 表单（[`admin.html`](../admin.html:1198-1202）
     4. 移除对应的 Vue data/properties 和 methods（`adminList`、`getSectionGroups`、`getGroupName` 等仍可能被其他功能引用，需确认依赖）
     5. 后端 sections API 文件可按需保留或移除

⏳ **22. 修复管理页面文件夹和文件删除按钮点击无反应**
   - 涉及文件: [`admin.html`](../admin.html)
   - 问题表现:
     - 文件夹侧栏中的 "删除目录" 按钮（[`admin.html`](../admin.html:675)）点击后通过 `deleteCurrentFolder` 方法执行，当前方法可能未正确处理文件夹下有文件的场景
     - 文件删除在网格/列表视图中通过 `handleDelete` 执行，之前已修复 GET→POST，但仍可能存在 KV 记录删除后前端列表未正确刷新的问题
     - 批量删除 `handleBatchDelete`（[`admin.html`](../admin.html:1235)）可能需要确认是否处理了跨存储类型（R2+S3+Telegram 混合）的删除
   - 建议排查:
     1. 确认 `deleteCurrentFolder` 方法（约 line 2660-2680）是否正确调用了后端删除 API（`/api/manage/files/move-folder` 或专用删除端点）
     2. 确认删除后 `tableData`、`folderTreeNodes` 是否完整刷新
     3. 确认批量删除时是否对每个文件正确调用了 `handleDelete` 或 `/api/manage/delete/[id]`

⏳ **23. 拆分大 HTML 文件，实现组件化架构**
   - 涉及文件:
     - [`admin.html`](../admin.html) — **2959 行**，单文件包含 6 个 Tab 面板 + 10 个模态框 + 完整 Vue 逻辑
     - [`index.html`](../index.html) — 2303 行
     - [`preview.html`](../preview.html) — 1918 行
     - [`gallery.html`](../gallery.html) — 1410 行
   - 问题: 单 HTML 文件包含所有模板、样式、脚本。admin.html 近 3000 行，维护困难、加载慢、无法并行开发
   - 建议拆分方案:
     ```
     admin/
     ├── admin.html              # 入口，仅加载依赖
     ├── admin.css               # 样式拆分
     ├── admin-mods.css          # 模态框/对话框样式
     ├── admin-app.js            # Vue 应用初始化 + 工具函数
     ├── components/
     │   ├── file-manager.js     # 文件管理 Tab 逻辑
     │   ├── user-manager.js     # 用户管理 Tab 逻辑
     │   ├── group-manager.js    # 身份组管理 Tab 逻辑
     │   ├── token-manager.js    # API Token 模态框逻辑
     │   ├── move-dialog.js      # 移动文件模态框逻辑
     │   └── preview-modal.js    # 预览模态框逻辑
     └── templates/
         ├── file-manager.html   # 文件管理模板
         ├── user-manager.html   # 用户管理模板
         └── modals.html         # 模态框模板
     ```
   - 步骤建议:
     1. 先将 CSS `<style>` 块拆分为独立 `.css` 文件
     2. 将工具函数（showToast、showConfirm、showPrompt 等）拆为独立 `.js`
     3. 按 Tab 维度拆分 Vue 组件的 template + methods
     4. 使用 Vue 2 的 `Vue.component()` 或 mixin 模式逐步组件化
     5. 最终引入构建工具（参考 18 号建议）实现真正的模块化
